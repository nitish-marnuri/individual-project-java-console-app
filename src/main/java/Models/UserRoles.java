package Models;

/*
The UserRoles enum defines the possible roles a member can have within a society.
It provided code clarity and also provides a type-safe way to manage fixed sets of constants
reducing errors.
*/

public enum UserRoles {
    ADMIN, // Administrative role
    MEMBER // Member role
}